# This rule is not used by the project ecoscape itself.
# It is provided to allow the cluster admin to help manage permissions for users.
#
# Grants permissions to create, update, and delete resources within the experiment.cau-se.de.
# This role is intended for users who need to manage these resources
# but should not control RBAC or manage permissions for others.

apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/name: ecoscape
    app.kubernetes.io/managed-by: kustomize
  name: experiment-editor-role
rules:
- apiGroups:
  - experiment.cau-se.de
  resources:
  - experiments
  verbs:
  - create
  - delete
  - get
  - list
  - patch
  - update
  - watch
- apiGroups:
  - experiment.cau-se.de
  resources:
  - experiments/status
  verbs:
  - get
